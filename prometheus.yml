global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'greencrest-api'
    http_headers:
      X-API-Key: 
      # PUT API KEY HERE
        values: [''] 

    static_configs:
      - targets: ['api:8080']
    metrics_path: '/metrics'
    scrape_interval: 5s
